import styled from "@emotion/styled";
import { GRID_STYLE } from "config";

export const SearchPostListWrapper = styled.div`
  .searched-post-wrapper {
    ${GRID_STYLE("1rem", "repeat(4,1fr)")}
  }
`;
