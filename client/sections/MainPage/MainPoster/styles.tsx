import styled from "@emotion/styled";
import { XLG_SIZE } from "config";
import tw from "twin.macro";

export const MainPosterWrapper = styled.div`
  width: ${XLG_SIZE};
  ${tw`mx-auto bg-white p-2 rounded-2xl`}
`;
